{
  "name": "lucify-embed-test-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./prepare-index",
    "start": "npm run-script build && ws -d www"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "local-web-server": "^1.2.5"
  }
}
